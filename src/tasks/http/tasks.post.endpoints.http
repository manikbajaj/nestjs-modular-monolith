POST http://localhost:3000/tasks
Content-Type: application/json

{
  "title": "Implement global validation pipe",
  "description": "Add a global validation pipe to the NestJS application to handle DTO validations.",
  "status": "pending"
}